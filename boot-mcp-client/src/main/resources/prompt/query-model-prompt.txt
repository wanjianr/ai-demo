用户查询需求：%s

请按照以下步骤处理这个查询：

1. 首先分析查询需求，识别可能涉及的表名及属性
2. 首先使用 getDatabaseTables 工具获取所有相关的表名
3. 然后使用 getDatabaseStructure 工具各表的结构、关联关系
4. 基于表结构生成合适的SQL查询语句
5. 使用 executeQuery 工具执行SQL,需要拼接分页参数 page:%s 和 pageSize:%s


请确保：
- SQL语句语法正确
- 使用反引号包围字段名和表名
- 添加适当的WHERE条件和ORDER BY排序
- 结果展示友好易读
